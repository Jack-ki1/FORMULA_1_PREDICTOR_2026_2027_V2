# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv
.venv-*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Env secrets — keep example, ignore real
.env
.env.local

# Cache — keep directory structure but ignore contents ( HF ephemeral disk is cheap, but don't bloat build context )
cache/api_responses/*.json
cache/fastf1_cache/*
cache/model_cache/*
*.db
*.sqlite
*.sqlite3

# Git
.git/
.gitignore

# OS
.DS_Store
Thumbs.db
*.log

# HF Space: keep README.md and dashboard/static/* (needed in image)
# Do NOT ignore *.md, *.png, *.mp4 — that broke Space in previous version

# Not needed for live image (save ~2MB + 60KB docs)
uv.lock
tests/
.pytest_cache/
.coverage
htmlcov/
__pycache__/
.mypy_cache/
.pytest_cache/
